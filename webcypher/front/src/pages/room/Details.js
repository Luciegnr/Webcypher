import { DetailsScreen } from '@components/room';
const Details = () => {
    return (
        <div className="ml4 mt4" style={{display: "flex"}}>
            <DetailsScreen style={{display: "flex", margin: "auto"}} />
        </div>
    );
};

export default Details;
